# Fake Repo

This fake repository is used by the AI Agent Safety Toolkit full workflow demo.

## CLI Example

Incorrect example:

```bash
node src/cli.js --name demo
```

Expected example:

```bash
node src/index.js demo
```

## Safety

This fixture does not require network access, external accounts, secrets, cookies, tokens, keychains, payment flows, KYC, or real user data.
